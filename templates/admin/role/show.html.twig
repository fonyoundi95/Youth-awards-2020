{% extends 'admin/base.html.twig' %}


{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ role.title }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('role_index') }}">back to list</a>

<a class="btn btn-success" href="{{ path('role_edit', {'id': role.id}) }}">edit</a>


{{ include('admin/role/_delete_form.html.twig') }}

{% endblock %}
