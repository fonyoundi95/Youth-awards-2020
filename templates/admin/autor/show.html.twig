{% extends 'admin/base.html.twig' %}


{% block title %}Autor
{% endblock %}

{% block body %}
<h1class="mt-5">Autor</h1>


	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ autor.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ autor.name }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ autor.email }}</td>
			</tr>
			<tr>
				<th>Passeword</th>
				<td>{{ autor.passeword }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-info" href="{{ path('autor_index') }}">back to list</a>


	<a class="btn btn-success" href="{{ path('autor_edit', {'id': autor.id}) }}">edit</a>

{{ include('admin/autor/_delete_form.html.twig') }}

{% endblock %}
