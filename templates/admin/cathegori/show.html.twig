{% extends 'admin/base.html.twig' %}


{% block title %}
  Cathegori
{% endblock %}

{% block body %}
  <h1 class="mt-5">
    Cathegori
  </h1>

  <table class="table">
    <tbody>
      <tr>
        <th>
          Id
        </th>
        <td>
          {{ cathegori.id }}
        </td>
      </tr>
      <tr>
        <th>
          Title
        </th>
        <td>
          {{ cathegori.title }}
        </td>
      </tr>
      <tr>
        <th>
          Imag
        </th>
        <td>
          {{ cathegori.imag }}
        </td>
      </tr>
    </tbody>
  </table>

  <a class="btn btn-info" href="{{ path('cathegori_index') }}">back to list</a>

  <a class="btn btn-success"
    href="{{
    path(
      'cathegori_edit',
      {
        id: cathegori.id
      }
    )
    }}">
    edit
  </a>

{{ include('admin/cathegori/_delete_form.html.twig') }}

{% endblock %}
